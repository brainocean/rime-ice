patch:
  show_notifications_when: always # 状态通知，可设为全开（always）全关（never）  

  style/color_scheme: apathy
  style/candidate_list_layout: linear  # stacked | linear

  app_options:
    com.neovide.neovide:
      ascii_mode: true  # 初始爲西文模式
      no_inline: true   # 不使用行內編輯
      vim_mode: true    # 退出VIM插入模式自動切換輸入法狀態
    md.obsidian:
      ascii_mode: true  # 初始爲西文模式
      no_inline: true   # 不使用行內編輯
      vim_mode: true    # 退出VIM插入模式自動切換輸入法狀態
    net.kovidgoyal.kitty:
      ascii_mode: true
      no_inline: true
      vim_mode: true
    neovide:
      ascii_mode: true
      no_inline: true
      vim_mode: true
    nvim:
      ascii_mode: true
      no_inline: true
      vim_mode: true

